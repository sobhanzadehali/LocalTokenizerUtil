[project]
name = "token-utils"
version = "0.1.0"
authors = [
    { name = "sobhan", email = "sobhan@local.com" }
]
description = "A utility library for tokenization"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "transformers>=5.7.0",
]
[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"
